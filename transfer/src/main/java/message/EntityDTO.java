package message;

/**
 * Marker class for all entities than can be send or receive via message system.
 * Not an interface because this type is used in JSONSerializer which required no-args constructor.
 */
public class EntityDTO {
}
